<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Feed Me a Movie - AI-Powered Recommendations</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --green: #1DB954;
            --green-light: #1ed760;
            --green-dark: #1aa34a;
            --purple: #8B5CF6;
            --blue: #3B82F6;
            --teal: #14B8A6;
            --cyan: #06B6D4;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: linear-gradient(135deg, #0f0a08 0%, #1a1410 25%, #12090a 50%, #1f1108 75%, #0f0a08 100%);
            background-size: 400% 400%;
            animation: atmosphericGradient 15s ease infinite;
            min-height: 100vh;
            font-family: 'DM Sans', -apple-system, BlinkMacSystemFont, sans-serif;
            color: #ffffff;
            position: relative;
            overflow-x: hidden;
        }

        @keyframes atmosphericGradient {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* Atmospheric overlay - warm cinematic tones */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background:
                radial-gradient(circle at 20% 30%, rgba(255, 140, 66, 0.08) 0%, transparent 50%),
                radial-gradient(circle at 80% 70%, rgba(214, 137, 16, 0.08) 0%, transparent 50%),
                radial-gradient(circle at 50% 50%, rgba(139, 46, 31, 0.06) 0%, transparent 70%);
            pointer-events: none;
            z-index: 1;
        }

        /* Subtle noise texture */
        body::after {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 400 400' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='3' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
            opacity: 0.015;
            pointer-events: none;
            z-index: 1;
        }

        .content-wrapper {
            position: relative;
            z-index: 2;
        }

        /* Typography */
        .logo-text {
            font-weight: 700;
            font-size: 3rem;
            letter-spacing: -0.02em;
            background: linear-gradient(135deg, #ffffff 0%, #ffd4a3 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        /* Movie Card */
        .movie-card {
            transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
            background: rgba(20, 13, 10, 0.6);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 200, 150, 0.15);
            height: 100%;
            overflow-y: auto;
            overflow-x: hidden;
            box-shadow:
                0 20px 60px rgba(0, 0, 0, 0.6),
                0 0 80px rgba(255, 140, 66, 0.15),
                inset 0 1px 0 rgba(255, 200, 150, 0.1);
            animation: cardEntrance 0.6s cubic-bezier(0.4, 0, 0.2, 1) forwards;
        }

        @keyframes cardEntrance {
            from {
                opacity: 0;
                transform: scale(0.95) translateY(20px);
            }
            to {
                opacity: 1;
                transform: scale(1) translateY(0);
            }
        }

        .movie-card::-webkit-scrollbar {
            width: 4px;
        }

        .movie-card::-webkit-scrollbar-track {
            background: rgba(255, 255, 255, 0.05);
        }

        .movie-card::-webkit-scrollbar-thumb {
            background: var(--green);
            border-radius: 10px;
        }

        .movie-card.swipe-left {
            transform: translateX(-120%) rotate(-20deg) scale(0.85);
            opacity: 0;
        }

        .movie-card.swipe-right {
            transform: translateX(120%) rotate(20deg) scale(0.85);
            opacity: 0;
        }

        /* Glass card style */
        .glass-card {
            background: rgba(15, 23, 42, 0.4);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
        }

        /* Badges */
        .badge {
            display: inline-block;
            padding: 0.4rem 0.9rem;
            border-radius: 8px;
            font-size: 0.75rem;
            font-weight: 600;
            margin: 0.25rem;
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
            color: #e0e7ff;
            backdrop-filter: blur(10px);
        }

        .badge-source {
            background: linear-gradient(135deg, rgba(214, 137, 16, 0.3) 0%, rgba(255, 140, 66, 0.3) 100%);
            border: 1px solid rgba(214, 137, 16, 0.5);
            color: #ffd4a3;
        }

        .badge-rewatch {
            background: linear-gradient(135deg, rgba(251, 146, 60, 0.3) 0%, rgba(249, 115, 22, 0.3) 100%);
            border: 1px solid rgba(251, 146, 60, 0.5);
            color: #fed7aa;
        }

        /* Buttons */
        .btn {
            font-weight: 600;
            letter-spacing: -0.01em;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.2);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }

        .btn:hover::before {
            width: 300px;
            height: 300px;
        }

        .green-btn {
            background: linear-gradient(135deg, var(--green) 0%, var(--green-dark) 100%);
            color: #000000;
            border: 1px solid var(--green);
            box-shadow: 0 0 30px rgba(29, 185, 84, 0.3);
        }

        .green-btn:hover {
            box-shadow: 0 0 50px rgba(29, 185, 84, 0.5);
            transform: translateY(-2px);
        }

        .green-btn:active {
            transform: translateY(0);
        }

        .dark-btn {
            background: rgba(30, 41, 59, 0.6);
            color: #e0e7ff;
            border: 1px solid rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(10px);
        }

        .dark-btn:hover {
            background: rgba(51, 65, 85, 0.7);
            border-color: var(--green);
            box-shadow: 0 0 20px rgba(29, 185, 84, 0.2);
        }

        /* Genre buttons */
        .genre-btn {
            background: rgba(30, 41, 59, 0.5);
            backdrop-filter: blur(10px);
            color: #e0e7ff;
            padding: 1rem;
            border-radius: 12px;
            border: 1px solid rgba(255, 255, 255, 0.15);
            font-weight: 600;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }

        .genre-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(29, 185, 84, 0.1), transparent);
            transition: left 0.5s;
        }

        .genre-btn:hover::before {
            left: 100%;
        }

        .genre-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 24px rgba(29, 185, 84, 0.2);
            border-color: var(--green);
        }

        .genre-btn.selected {
            background: linear-gradient(135deg, var(--green) 0%, var(--green-dark) 100%);
            color: #000000;
            border-color: var(--green);
            box-shadow: 0 0 30px rgba(29, 185, 84, 0.4);
        }

        /* Tab styling */
        .tab-btn {
            background: transparent;
            color: rgba(224, 231, 255, 0.6);
            padding: 0.75rem 2rem;
            border-radius: 12px;
            border: 1px solid transparent;
            font-weight: 600;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            cursor: pointer;
            font-size: 0.9rem;
        }

        .tab-btn:hover {
            color: #ffffff;
            border-color: rgba(255, 255, 255, 0.2);
            background: rgba(255, 255, 255, 0.05);
        }

        .tab-btn.active {
            background: linear-gradient(135deg, var(--green) 0%, var(--green-dark) 100%);
            color: #000000;
            border-color: var(--green);
            box-shadow: 0 0 20px rgba(29, 185, 84, 0.3);
        }

        .badge-count {
            display: inline-block;
            background: rgba(0, 0, 0, 0.4);
            color: #e0e7ff;
            padding: 0.25rem 0.5rem;
            border-radius: 8px;
            font-size: 0.7rem;
            margin-left: 0.5rem;
        }

        .tab-btn.active .badge-count {
            background: rgba(0, 0, 0, 0.3);
            color: #000000;
        }

        /* Action buttons */
        .action-btn {
            width: 72px;
            height: 72px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            border: 2px solid;
            cursor: pointer;
            backdrop-filter: blur(10px);
        }

        .action-btn-skip {
            background: rgba(30, 41, 59, 0.6);
            border-color: rgba(255, 255, 255, 0.2);
            color: rgba(255, 255, 255, 0.8);
        }

        .action-btn-skip:hover {
            background: rgba(51, 65, 85, 0.7);
            border-color: rgba(255, 255, 255, 0.4);
            color: white;
            transform: scale(1.1);
            box-shadow: 0 0 30px rgba(255, 255, 255, 0.15);
        }

        .action-btn-seen {
            background: rgba(30, 41, 59, 0.6);
            border-color: rgba(251, 146, 60, 0.4);
            color: #fed7aa;
        }

        .action-btn-seen:hover {
            background: rgba(251, 146, 60, 0.2);
            border-color: rgba(251, 146, 60, 0.6);
            transform: scale(1.1);
            box-shadow: 0 0 30px rgba(251, 146, 60, 0.3);
        }

        .action-btn-like {
            background: linear-gradient(135deg, var(--green) 0%, var(--green-dark) 100%);
            border-color: var(--green);
            color: #000000;
            box-shadow: 0 0 30px rgba(29, 185, 84, 0.4);
        }

        .action-btn-like:hover {
            transform: scale(1.15);
            box-shadow: 0 0 50px rgba(29, 185, 84, 0.6);
        }

        .action-btn:active {
            transform: scale(0.95);
        }

        /* Streaming logos */
        .streaming-service {
            position: relative;
            display: inline-block;
        }

        .streaming-logo {
            height: 36px;
            width: auto;
            border-radius: 6px;
            filter: brightness(1.1) contrast(1.05);
            opacity: 1 !important;
            transition: all 0.3s;
        }

        .streaming-logo:hover {
            filter: brightness(1.3) contrast(1.1);
            transform: translateY(-2px);
        }

        .rental-indicator {
            position: absolute;
            bottom: -6px;
            left: 50%;
            transform: translateX(-50%);
            background: var(--green);
            color: #000000;
            font-size: 9px;
            font-weight: bold;
            padding: 2px 5px;
            border-radius: 4px;
        }

        /* Star rating */
        .star-wrapper {
            position: relative;
            cursor: pointer;
            display: inline-block;
            transition: transform 0.2s;
        }

        .star-wrapper:hover {
            transform: scale(1.15);
        }

        .rating-star {
            color: rgba(255, 255, 255, 0.2);
            transition: all 0.3s;
            user-select: none;
        }

        .star-wrapper:hover .rating-star {
            color: var(--green);
            filter: drop-shadow(0 0 10px rgba(29, 185, 84, 0.5));
        }

        .star-wrapper.filled .rating-star {
            color: var(--green);
            filter: drop-shadow(0 0 8px rgba(29, 185, 84, 0.3));
        }

        .star-wrapper.half-filled .rating-star {
            background: linear-gradient(90deg, var(--green) 50%, rgba(255, 255, 255, 0.2) 50%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        /* Watchlist */
        .watchlist-item {
            background: rgba(30, 41, 59, 0.5);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 16px;
            padding: 1.5rem;
            display: flex;
            gap: 1.5rem;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .watchlist-item:hover {
            border-color: var(--green);
            transform: translateY(-4px);
            box-shadow: 0 12px 40px rgba(29, 185, 84, 0.2);
        }

        .watchlist-poster {
            width: 100px;
            height: 150px;
            object-fit: cover;
            border-radius: 12px;
            flex-shrink: 0;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        /* Loading animation */
        .loading-spinner {
            width: 64px;
            height: 64px;
            border: 3px solid rgba(29, 185, 84, 0.2);
            border-top: 3px solid var(--green);
            border-radius: 50%;
            animation: spin 0.8s cubic-bezier(0.4, 0, 0.2, 1) infinite;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Modal */
        .modal-overlay {
            backdrop-filter: blur(20px);
            background: rgba(10, 14, 39, 0.8);
            animation: fadeIn 0.3s ease-out;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .modal-content {
            animation: modalSlideUp 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        }

        @keyframes modalSlideUp {
            from {
                opacity: 0;
                transform: translateY(40px) scale(0.95);
            }
            to {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }

        /* Match percentage badge */
        .match-badge {
            background: linear-gradient(135deg, var(--green) 0%, var(--green-dark) 100%);
            color: #000000;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            font-weight: 700;
            font-size: 0.85rem;
            box-shadow: 0 0 20px rgba(29, 185, 84, 0.4);
            border: 1px solid var(--green-light);
        }

        /* Fade in animations */
        .fade-in {
            animation: fadeInUp 0.6s cubic-bezier(0.4, 0, 0.2, 1) forwards;
            opacity: 0;
        }

        .fade-in-1 { animation-delay: 0.1s; }
        .fade-in-2 { animation-delay: 0.2s; }
        .fade-in-3 { animation-delay: 0.3s; }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Poster hover effect */
        .poster-container {
            position: relative;
            overflow: hidden;
        }

        .poster-container::after {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 50%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.15), transparent);
            transition: left 0.6s;
        }

        .poster-container:hover::after {
            left: 150%;
        }

        /* Section divider */
        .section-divider {
            height: 1px;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            margin: 1.5rem 0;
        }

        /* Section header */
        .section-header {
            font-size: 0.7rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            color: var(--green);
            margin-bottom: 0.75rem;
        }
    </style>
</head>
<body>
    <div class="content-wrapper container mx-auto px-4 py-12 max-w-4xl">
        <!-- Header -->
        <div class="text-center mb-12 fade-in fade-in-1">
            <h1 class="logo-text mb-4">🎬 Feed Me a Movie</h1>
            <p class="text-gray-400 text-base mt-4 font-normal">Finding movies as your food gets cold is disappointing.<br>Find a movie you actually want to watch quicker</p>
        </div>

        <!-- Navigation Tabs -->
        <div class="flex justify-center gap-4 mb-10 fade-in fade-in-2">
            <button onclick="switchTab('discover')" id="discover-tab" class="tab-btn active">
                Discover
            </button>
            <button onclick="switchTab('watchlist')" id="watchlist-tab" class="tab-btn">
                Watchlist <span id="watchlist-count" class="badge-count">0</span>
            </button>
            <button onclick="changeGenres()" id="change-genres-btn" class="tab-btn" style="display: none;">
                Change Genres
            </button>
        </div>

        <!-- Genre Selection Screen -->
        <div id="genre-selection" class="glass-card rounded-2xl p-10 fade-in fade-in-3">
            <h2 class="text-3xl font-bold text-center mb-3">What are you feeling like watching?</h2>
            <p class="text-gray-400 text-center mb-10">Select one or more genres to get started</p>

            <div class="grid grid-cols-2 md:grid-cols-3 gap-3 mb-10">
                <button onclick="toggleGenre('Action')" class="genre-btn" data-genre="Action">Action</button>
                <button onclick="toggleGenre('Comedy')" class="genre-btn" data-genre="Comedy">Comedy</button>
                <button onclick="toggleGenre('Drama')" class="genre-btn" data-genre="Drama">Drama</button>
                <button onclick="toggleGenre('Thriller')" class="genre-btn" data-genre="Thriller">Thriller</button>
                <button onclick="toggleGenre('Romance')" class="genre-btn" data-genre="Romance">Romance</button>
                <button onclick="toggleGenre('Sci-Fi')" class="genre-btn" data-genre="Sci-Fi">Sci-Fi</button>
                <button onclick="toggleGenre('Horror')" class="genre-btn" data-genre="Horror">Horror</button>
                <button onclick="toggleGenre('Documentary')" class="genre-btn" data-genre="Documentary">Documentary</button>
                <button onclick="toggleGenre('Animation')" class="genre-btn" data-genre="Animation">Animation</button>
                <button onclick="toggleGenre('Crime')" class="genre-btn" data-genre="Crime">Crime</button>
                <button onclick="toggleGenre('Mystery')" class="genre-btn" data-genre="Mystery">Mystery</button>
                <button onclick="toggleGenre('Adventure')" class="genre-btn" data-genre="Adventure">Adventure</button>
            </div>

            <div class="text-center">
                <button onclick="startRecommendations()" class="btn green-btn px-10 py-4 rounded-xl font-bold" id="start-btn" disabled>
                    Start Finding Movies
                </button>
                <p class="text-gray-500 text-sm mt-4" id="genre-hint">Select at least one genre</p>
            </div>
        </div>

        <!-- Stats -->
        <div id="stats" class="glass-card rounded-xl p-6 mb-8 text-center" style="display: none;">
            <div class="flex justify-around">
                <div>
                    <div class="text-3xl font-bold" style="color: var(--green);" id="remaining-count">-</div>
                    <div class="text-xs text-gray-500 uppercase tracking-wider mt-1">Remaining</div>
                </div>
                <div>
                    <div class="text-3xl font-bold" style="color: var(--green);" id="liked-count">0</div>
                    <div class="text-xs text-gray-500 uppercase tracking-wider mt-1">Liked</div>
                </div>
                <div>
                    <div class="text-3xl font-bold text-gray-500" id="skipped-count">0</div>
                    <div class="text-xs text-gray-500 uppercase tracking-wider mt-1">Skipped</div>
                </div>
            </div>
        </div>

        <!-- Loading State -->
        <div id="loading" class="text-center py-24" style="display: none;">
            <div class="loading-spinner mx-auto mb-6"></div>
            <p class="text-gray-400">Loading recommendations...</p>
        </div>

        <!-- Movie Card Stack -->
        <div id="card-container" class="relative" style="height: 800px; display: none;">
            <!-- Cards will be dynamically inserted here -->
        </div>

        <!-- No More Movies -->
        <div id="no-more" class="text-center py-24" style="display: none;">
            <div class="text-6xl mb-6">🎉</div>
            <h2 class="text-3xl font-bold mb-3">All done!</h2>
            <p class="text-gray-400 mb-8" id="no-more-description">You've reviewed all recommendations</p>
            <button id="see-more-btn" onclick="generateMoreRecommendations()" class="btn green-btn px-8 py-4 rounded-xl font-bold">
                See More
            </button>
            <p class="text-gray-500 text-sm mt-3" id="see-more-hint">Loading more movies in your selected genres</p>
        </div>

        <!-- Action Buttons -->
        <div id="actions" class="flex justify-center items-center gap-6 mt-10" style="display: none;">
            <button onclick="swipeLeft()" class="action-btn action-btn-skip" title="Skip">
                <svg class="w-9 h-9" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"></path>
                </svg>
            </button>
            <button onclick="showAlreadySeenModal()" class="action-btn action-btn-seen" title="Already Seen">
                <svg class="w-9 h-9" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                    <path stroke-linecap="round" stroke-linejoin="round" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                </svg>
            </button>
            <button onclick="swipeRight()" class="action-btn action-btn-like" title="Add to Watchlist">
                <svg class="w-9 h-9" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"></path>
                </svg>
            </button>
        </div>

        <!-- Keyboard Hints -->
        <div id="keyboard-hints" class="text-center text-gray-600 text-xs mt-8 tracking-wide" style="display: none;">
            ← SKIP  •  S ALREADY SEEN  •  → ADD TO WATCHLIST
        </div>

        <!-- Watchlist View -->
        <div id="watchlist-view" style="display: none;">
            <div id="watchlist-empty" class="text-center py-24" style="display: none;">
                <div class="text-6xl mb-6">📋</div>
                <h2 class="text-3xl font-bold mb-3">Your watchlist is empty</h2>
                <p class="text-gray-400 mb-8">Movies you like will appear here</p>
                <button onclick="switchTab('discover')" class="btn green-btn px-8 py-4 rounded-xl font-bold">
                    Discover Movies
                </button>
            </div>

            <div id="watchlist-grid" class="grid grid-cols-1 gap-5">
                <!-- Watchlist items will be dynamically inserted here -->
            </div>
        </div>

        <!-- Already Seen Modal -->
        <div id="already-seen-modal" class="modal-overlay fixed inset-0 flex items-center justify-center" style="display: none; z-index: 1000;">
            <div class="modal-content glass-card rounded-2xl p-10 max-w-md w-full mx-4">
                <h2 class="text-3xl font-bold mb-4">Rate this movie</h2>
                <p class="text-gray-300 mb-2 font-medium" id="modal-movie-title"></p>
                <p class="text-gray-500 text-sm mb-8">How would you rate it?</p>

                <!-- Star Rating -->
                <div class="flex justify-center gap-2 mb-10" id="star-rating-container">
                    <div class="star-wrapper" data-star="1" onclick="handleStarClick(event, 1)">
                        <span class="rating-star text-6xl">★</span>
                    </div>
                    <div class="star-wrapper" data-star="2" onclick="handleStarClick(event, 2)">
                        <span class="rating-star text-6xl">★</span>
                    </div>
                    <div class="star-wrapper" data-star="3" onclick="handleStarClick(event, 3)">
                        <span class="rating-star text-6xl">★</span>
                    </div>
                    <div class="star-wrapper" data-star="4" onclick="handleStarClick(event, 4)">
                        <span class="rating-star text-6xl">★</span>
                    </div>
                    <div class="star-wrapper" data-star="5" onclick="handleStarClick(event, 5)">
                        <span class="rating-star text-6xl">★</span>
                    </div>
                </div>

                <div class="text-center mb-8">
                    <span class="text-4xl font-bold" style="color: var(--green);" id="selected-rating">-</span>
                    <span class="text-gray-500 text-xl"> / 5.0</span>
                </div>

                <!-- Action Buttons -->
                <div class="flex gap-4">
                    <button onclick="closeModal()" class="flex-1 btn dark-btn px-6 py-4 rounded-xl font-bold">
                        Cancel
                    </button>
                    <button onclick="submitRating()" class="flex-1 btn green-btn px-6 py-4 rounded-xl font-bold" id="submit-rating-btn" disabled>
                        Submit
                    </button>
                </div>
            </div>
        </div>
    </div>

    <script src="app.js"></script>
</body>
</html>
